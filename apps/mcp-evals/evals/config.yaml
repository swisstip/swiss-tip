configs:
  opencode-apertus:
    harness: opencode
    model: gpt-5
    command: [opencode, run, "--format", "json", "--model", "{model}", "{question}"]
  codex-gpt:
    harness: codex
    model: gpt-5.5
    command: [codex, --config, web_search="disabled", exec, --json, --model, "{model}", "{question}"]
benchmark:
  trials: 1
  concurrency: 1