"""Swiss TIP runtime: get_coverage, search, resolve and get_evidence over a release.

Pure functions over an in-memory, validated release. No network, no model;
the MCP server app wraps this package for the wire.
"""
